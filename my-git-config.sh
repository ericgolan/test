#!/bin/bash
# updaing the file ~/.gitconfig
# note that --add will create more of the same, remove --add to change existing
# git config --global --add user.name "Eric Golan"
git config --global "Eric Golan"
# git config --global --add user.email "golan@harel.co.il"
git config --global "golan@harel.co.il"
